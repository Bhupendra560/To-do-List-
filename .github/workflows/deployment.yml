# name: Deploy to Production

# on:
#   push:
#     branches:
#       - release

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     env:
#       DB_DATABASE: mytest_db
#       DB_USER: newuser
#       DB_PASSWORD: password
#       PYTHONANYWHERE_USER: ${{ secrets.PYTHONANYWHERE_USER }}
#       PYTHONANYWHERE_PASSWORD: ${{ secrets.PYTHONANYWHERE_PASSWORD }}

#   steps:
#     - name: Checkout code
#       uses: actions/checkout@v2
#       with:
#         path: todolist

#     - name: Set up Python
#       uses: actions/setup-python@v3
#       with:
#         python-version: 3.x

#     - name: Install dependencies
#       run: |
#         cd todolist/todolist
#         pip install -r requirements.txt

#     - name: Deploy to PythonAnywhere
#       run: |
#         # Use environment variables to configure the Django application
#         echo "DJANGO_ENV=production" >> $GITHUB_ENV
#         echo "DATABASE_URL=mysql://${DB_USER}:${DB_PASSWORD}@localhost:3306/${DB_DATABASE}" >> $GITHUB_ENV
        
#         # Add PythonAnywhere environment variables
#         echo "PYTHONANYWHERE_USER=${PYTHONANYWHERE_USER}" >> $GITHUB_ENV
#         echo "PYTHONANYWHERE_PASSWORD=${PYTHONANYWHERE_PASSWORD}" >> $GITHUB_ENV
        
#         # Run Django migrations and collect static files
#         cd todolist/todolist
#         python manage.py migrate
#         python manage.py collectstatic

#         # Deploy to PythonAnywhere (replace 'your_username' with your PythonAnywhere username)
#         pa_autoconfigure_django.py --python=3.8 https://github.com/your_username/your_repository.git
